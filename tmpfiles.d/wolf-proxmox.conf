# Bind-mount source dirs Wolf needs on the PVE host. /run is tmpfs, so /run/wolf
# must be recreated on boot; /etc/wolf is persistent (created if missing).
# Install: cp to /etc/tmpfiles.d/ then `systemd-tmpfiles --create`.
d /etc/wolf 0755 root root -
d /run/wolf 0755 root root -
